# Schema file not used other than for adding type hints in the IDE.

type Query {
    site: Site
}
type Site {
    buildTime(formatString: String): String
    siteMetadata: SiteMetadata
}
type SiteMetadata {
    title: String
    description: String
    author: String
}
